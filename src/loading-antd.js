import Vue from 'vue';

import {
  Button,
  Row,
  Col,
  Menu,
  Icon,
  Layout,
  Select,
  Dropdown,
  Tabs,
  Avatar
} from 'ant-design-vue';

Vue.use(Button)
  .use(Row)
  .use(Col)
  .use(Menu)
  .use(Icon)
  .use(Layout)
  .use(Select)
  .use(Dropdown)
  .use(Tabs)
  .use(Avatar);
